# OpenSky Network OAuth2 client credentials.
# Create at https://opensky-network.org/  ->  Account  ->  API Client
# Then: `export $(grep -v '^#' .env | xargs)` or source these into your shell.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
